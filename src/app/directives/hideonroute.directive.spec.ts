import { HideonrouteDirective } from './hideonroute.directive';

describe('HideonrouteDirective', () => {
  it('should create an instance', () => {
    const directive = new HideonrouteDirective();
    expect(directive).toBeTruthy();
  });
});
